{
  "name": "cypress-cucumber-project",
  "version": "1.0.0",
  "description": "Cypress + Cucumber BDD test automation setup",
  "scripts": {
    "test": "cypress run",
    "open": "cypress open"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^15.5.0",
    "esbuild": "^0.25.11"
  }
}
